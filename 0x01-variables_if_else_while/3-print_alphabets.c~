#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0
 */

int main(void)
{

  
   char upper = 'A';

   for (char lower = 'a'; lower <= 'z'; lower++)
     putchar(lower);
   for (char upper = 'A'; upper <= 'Z'; lower++)
}
