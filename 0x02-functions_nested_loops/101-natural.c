#include <stdio.h>

/**
 * main - program that prints the sum of all the multiples of 3 or 5 below 1024
 *
 * Return: 0
 */

int main(void)
{
int num;
int sum;

for (num = 0; num < 1024; num++)
{
if ((n % 3 == 0) || (n % 5 == 0))
{
sum = 0;
sum += num;
}
}
printf("%d\n", sum);
return (0);
}
